export interface ProgressItem {
  id: string;
  thumbnail: string;
  title: string;
  progress: number;
}

export interface ColoringItemData {
  id: string;
  imageUrl: string;
  title: string;
  category: string;
}
